{
  "name": "nhlcheck",
  "version": "1.0.0",
  "description": "NHL stats and predictions web app",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
